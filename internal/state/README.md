# Some notes on state

> State fields are intentionally kept private so that there is a clear, one way data flow between the objects.
> ID objects are kept public for simplicity since they are immutable
